# Set client credentials

# Set up an http server listening on localhost

# Open a browser showing the oauth uri and await approval on server

# Upon approval complete the second step of oauth

# Check connected

# Store user creds on default location on disk

# Strore client creds on default location on disk